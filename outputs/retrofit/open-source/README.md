# Open Source Retrofit Outputs

Generated outputs for open-source retrofit dogfood live here.

Current target:

- `cjson/`
  - read-only graph-source and planned instruction-pack dogfood for cJSON
- `escape-html/`
  - real `component/escape-html` local checkout dogfood with Symbol input behavior change, npm test pass, graph delta,
    graph update proposal, and dogfood report
- `todo-vue/`
  - real `mdn/todo-vue` local checkout dogfood with README-only instruction pack, graph delta, graph update proposal,
    dogfood report, and external baseline-tooling blocker separation
