# Native PBE Dogfood Outputs

Native dogfood outputs prove the same operation chain without inferred legacy
intent:

```text
graph-source -> instruction pack -> local change -> graph delta
```

Current target:

- `graph-notes-demo/` - README-only native maintenance boundary dogfood.
- `graph-counter-demo/` - tiny code behavior dogfood with a matching runtime test.
