# 00 Before

The target repository is an empty or minimal frontend project.

There is no `.pbe/` folder yet.

Known constraints:

- No backend is selected for the first slice.
- Persistence should use browser storage unless the Product Tree later changes that decision.
- The first slice should stay small enough for one review.

PBE starts with:

```text
@project-blueprint-engine start
```
