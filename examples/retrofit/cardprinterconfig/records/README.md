# CardPrinterConfig Retrofit Records

Each JSON file in this directory should validate against:

```text
schemas/retrofit/change-record-v0.json
```

The current records are copied from the live dogfood reports and normalized into
example fixture names:

- `smart51-test-setting.validated-then-reverted.json`
- `laminator-tag-layout.active.json`

The corresponding Markdown files are reader-facing summaries of the same
records.
